name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  # 1) Build, scan, and push the image (cloud runner — has internet access).
  image:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - id: meta
        run: echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT" # e.g. v1.2.3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          push: true
          tags: |
            d4knezsl1me/chri-portfolio:${{ steps.meta.outputs.tag }}
            d4knezsl1me/chri-portfolio:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trivy image scan (fail on HIGH/CRITICAL)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: d4knezsl1me/chri-portfolio:${{ steps.meta.outputs.tag }}
          severity: HIGH,CRITICAL
          exit-code: "1"
          ignore-unfixed: true
          trivyignores: .trivyignore

  # 2) Deploy to the cluster (self-hosted runner on naked-glados).
  deploy:
    needs: image
    runs-on: [self-hosted, naked-glados]
    steps:
      - uses: actions/checkout@v4

      - name: Apply manifests & pin image tag
        env:
          TAG: ${{ needs.image.outputs.tag }}
        run: |
          # Uses kubectl's built-in kustomize (-k); no standalone kustomize needed.
          kubectl apply -k deploy/k8s
          kubectl -n apps set image deploy/chri-portfolio web=d4knezsl1me/chri-portfolio:"$TAG"
          kubectl -n apps rollout status deploy/chri-portfolio --timeout=120s
